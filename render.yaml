services:
  - type: web
    name: malaria-detection-app
    env: python
    buildCommand: 
      - pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: PORT
        value: 5000
